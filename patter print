#include <iostream>
using namespace std;

int main()
{
	int n;
	cout<<"enter no of rows : ";
	cin>>n;
	cout<<1;
	
	for (int i=1;i<=n;i++)
	{
		for (int j=1;j<=i;j++)
		{
			if(i==1)
			{
				break;
			}
			if ((j==1) || (j==i))
			{
				cout<<i-1;
			}
			
			else cout<<0;
		}
		cout<<endl;
	}
}
